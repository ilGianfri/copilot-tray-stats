<Window x:Class="CopilotTrayStats.Views.UsageHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Copilot Stats — Usage History"
        Width="480" SizeToContent="Height"
        MinHeight="200"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen">

    <Border Margin="8" CornerRadius="8"
            Background="{StaticResource CardBg}"
            BorderBrush="{StaticResource BorderColor}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.6" ShadowDepth="4"/>
        </Border.Effect>

        <StackPanel>

            <!-- ── Title bar ── -->
            <Border Background="{StaticResource SubtleBg}" CornerRadius="8,8,0,0"
                    Padding="14,10" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Usage History"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="13" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="✕" Width="22" Height="22"
                            Background="Transparent" Foreground="{StaticResource TextSecondary}"
                            BorderThickness="0" FontSize="11" Cursor="Hand"
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <!-- ── Body ── -->
            <StackPanel Margin="16,14,16,16">

                <!-- Subtitle -->
                <TextBlock x:Name="SubtitleText"
                           Foreground="{StaticResource TextSecondary}"
                           FontSize="11" Margin="0,0,0,14"/>

                <!-- Chart area -->
                <Border Background="{StaticResource SubtleBg}" CornerRadius="6"
                        Padding="12,12,12,8" MinHeight="130">
                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Disabled">
                        <ItemsControl x:Name="BarsControl">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Width="32" Margin="2,0">
                                        <!-- Bar column: fixed 100px tall, bar sits at bottom -->
                                        <Grid Height="100">
                                            <Border VerticalAlignment="Bottom"
                                                    Height="{Binding BarHeight}"
                                                    CornerRadius="2,2,0,0"
                                                    ToolTip="{Binding Tooltip}">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="{StaticResource TextSecondary}"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource TextPrimary}"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <!-- Minimum visible height even if 0 used -->
                                                <Border.MinHeight>2</Border.MinHeight>
                                            </Border>
                                        </Grid>
                                        <!-- Date label -->
                                        <TextBlock Text="{Binding Date, StringFormat='MMM d'}"
                                                   Foreground="{StaticResource TextSecondary}"
                                                   FontSize="9" HorizontalAlignment="Center"
                                                   Margin="0,4,0,0">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsToday}" Value="True">
                                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Border>

                <!-- Empty state -->
                <TextBlock x:Name="EmptyText"
                           Text="No usage data yet. Data is recorded each time Copilot Stats refreshes."
                           Foreground="{StaticResource TextSecondary}"
                           FontSize="11" TextWrapping="Wrap"
                           HorizontalAlignment="Center" TextAlignment="Center"
                           Margin="0,8,0,0"
                           Visibility="Collapsed"/>

            </StackPanel>
        </StackPanel>
    </Border>
</Window>
