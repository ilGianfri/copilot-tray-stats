<Window x:Class="CopilotTrayStats.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:CopilotTrayStats.ViewModels"
        Title="Copilot Stats — Settings"
        Width="400" SizeToContent="Height"
        ResizeMode="NoResize"
        ShowInTaskbar="True"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        WindowStartupLocation="CenterScreen">

    <Border Margin="8" CornerRadius="8"
            Background="{StaticResource CardBg}"
            BorderBrush="{StaticResource BorderColor}"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" Opacity="0.6" ShadowDepth="4"/>
        </Border.Effect>

        <StackPanel>

            <!-- ── Title bar ── -->
            <Border Background="{StaticResource SubtleBg}" CornerRadius="8,8,0,0"
                    Padding="14,10" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Settings"
                               Foreground="{StaticResource TextPrimary}"
                               FontSize="13" FontWeight="SemiBold"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="✕" Width="22" Height="22"
                            Background="Transparent" Foreground="{StaticResource TextSecondary}"
                            BorderThickness="0" FontSize="11" Cursor="Hand"
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>

            <!-- ── Body ── -->
            <StackPanel Margin="16,14,16,16">

                <!-- Run on startup -->
                <Border Background="{StaticResource SubtleBg}" CornerRadius="6"
                        Padding="12,10" Margin="0,0,0,10">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Run on startup"
                                       Foreground="{StaticResource TextPrimary}"
                                       FontSize="12" FontWeight="Medium"/>
                            <TextBlock Text="Launch when Windows starts"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                        <CheckBox Grid.Column="1" VerticalAlignment="Center"
                                  IsChecked="{Binding RunOnStartup}"/>
                    </Grid>
                </Border>

                <!-- Refresh interval -->
                <Border Background="{StaticResource SubtleBg}" CornerRadius="6"
                        Padding="12,10" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Refresh interval"
                                       Foreground="{StaticResource TextPrimary}"
                                       FontSize="12" FontWeight="Medium"/>
                            <TextBlock Text="How often to fetch data from GitHub"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontSize="11" Margin="0,2,0,0"/>
                        </StackPanel>
                        <ComboBox Grid.Column="1" Width="110" VerticalAlignment="Center"
                                  ItemsSource="{Binding RefreshOptions}"
                                  SelectedItem="{Binding SelectedRefreshOption}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Label}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>
                </Border>

                <!-- Action buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Cancel" Margin="0,0,8,0" Cursor="Hand"
                            Background="Transparent" Foreground="{StaticResource TextSecondary}"
                            BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                            Padding="12,5"
                            Command="{Binding CancelCommand}"/>
                    <Button Content="Save" Style="{StaticResource PrimaryButton}"
                            Command="{Binding SaveCommand}"/>
                </StackPanel>

                <!-- Version footer -->
                <TextBlock HorizontalAlignment="Center" Margin="0,12,0,0"
                           Foreground="{StaticResource TextSecondary}" FontSize="10">
                    <Run Text="copilot-tray-stats"/>
                    <Run Text="{x:Static vm:SettingsViewModel.AppVersion}"/>
                </TextBlock>

            </StackPanel>
        </StackPanel>
    </Border>
</Window>
